/**
 * ⚠️ PLIK NIEAKTUALNY — MOŻNA SKASOWAĆ.
 *
 * Meta tagi, Open Graph, Twitter Card i dane strukturalne są teraz generowane
 * przy buildzie i wstrzykiwane wprost do `index.html` — patrz `vite.config.ts`
 * (plugin `seoHtml`). Powód zmiany: scrapery Facebooka i LinkedIna oraz część
 * robotów nie wykonują JavaScriptu, więc tagi dokładane w runtime były dla nich
 * niewidoczne i udostępniony link nie miał ani tytułu, ani obrazka.
 *
 * Ten plik został opróżniony, bo nie mogłem go usunąć zdalnie
 * (narzędzia plikowe potrafią tylko zapisywać). Skasuj go ręcznie —
 * nic go nie importuje.
 */
export {}
